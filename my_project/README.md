

## Command List

~~~
npm init
npm install serve
// to make mock server
mkdir static
cd static
touch index.html
touch app.js
// to run server
cd ..
npx serve -p 5050 static
// to setup the prequently command
vi package.json ( add "static" under the "scripts" )
npm run static
~~~
